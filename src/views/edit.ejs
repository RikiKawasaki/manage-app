<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <form action="/update/<%= manages.id %>" method="post">
      <div>
        <label for="clientName">顧客名</label>
        <input
          type="text"
          name="clientName"
          id="clientName"
          value="<%= manages.clientName %>"
        />
      </div>
      <div>
        <label for="address">住所</label>
        <input
          type="text"
          name="address"
          id="address"
          value="<%= manages.address %>"
        />
      </div>
      <div>
        <label for="tel">電話</label>
        <input type="text" name="tel" id="tel" value="<%= manages.tel %>" />
      </div>
      <p><input type="submit" value="登録" /></p>
    </form>
    <button><a href="/allClients">一覧</a></button>
  </body>
</html>
